### Instructions:
You are an expert data analyst specializing in database querying.

Your task is to identify the minimal set of columns used to write a correct SQL query that answers the question.

The database schema provided to you has already been filtered to contain only relevant tables.
Do NOT decide which tables are relevant.
Your job is only to identify which columns from these provided tables are necessary for the SQL query.

Only include columns that are necessary for the SQL query.
Do not include irrelevant columns.
Do not include extra columns just because they appear in the same table.
Include join keys only when they are needed to connect tables in the SQL query.

### Database Schema:
The schema is organized into sections such as [DATABASE_ID], [SELECTED_TABLES], [FOREIGN_KEY_RELATIONSHIPS_WITHIN_SELECTED_TABLES], and [TABLE].

Each column line provides:
column_name | data_type | key_type | referenced_column | column_description | example_values

Important notes:
- key_type is one of: PRIMARY KEY, FOREIGN KEY, NONE, NOT_AVAILABLE
- referenced_column is a full column path such as TableName.ColumnName
- referenced_column is NONE when the source data shows the column is not a foreign key
- referenced_column is NOT_AVAILABLE when foreign key information is not provided in the source data
- the foreign key relationship section is NONE when the source data shows no relationships in scope
- column_description may be NOT_AVAILABLE
- example_values may be NOT_AVAILABLE
- Table names and column names must be used exactly as provided in the schema

### Hint:
The hint (if provided) may highlight columns, conditions, or relationships that are relevant to the question.
Use the hint as additional guidance, but rely primarily on the schema and the question.

### Output Requirements (STRICT):

Return ONLY a valid JSON object.

Format:
{
  "relevant_columns": {
    "table_name1": ["column1", "column2"],
    "table_name2": ["column3"],
    "table_name3": ["column4", "column5", "column6"]
  }
}

Rules (STRICT):
- Keys must be table names from the provided database schema.
- Values must be lists of column names from the schema.
- Use double quotes for all keys and strings.
- Do NOT invent tables or columns that do not exist in the schema.
- Do NOT include explanations.
- Do NOT include reasoning.
- Do NOT include markdown or code blocks.
- Do NOT include any text before or after the JSON.
- Only include columns required for writing the SQL query.
- If a table has no required columns, do NOT include that table.

If no columns can be confidently identified, return:
{}

You may reason internally to determine the correct columns, but do NOT output the reasoning content.
Only output the final JSON object.

### Example 1:
[DATABASE_ID]
department_management

[SELECTED_TABLES]
- department
- management

[FOREIGN_KEY_RELATIONSHIPS_WITHIN_SELECTED_TABLES]
- management.department_ID -> department.Department_ID

[TABLE] department
- Department_ID | INT | PRIMARY KEY | NONE | Unique identifier for a department. | NOT_AVAILABLE
- Name | TEXT | NONE | NONE | Department name. | NOT_AVAILABLE
- Creation | TEXT | NONE | NONE | Creation date or period. | NOT_AVAILABLE
- Ranking | INT | NONE | NONE | Department ranking. | NOT_AVAILABLE
- Budget_in_Billions | REAL | NONE | NONE | Department budget. | NOT_AVAILABLE
- Num_Employees | REAL | NONE | NONE | Number of employees. | NOT_AVAILABLE

[TABLE] management
- department_ID | INT | FOREIGN KEY | department.Department_ID | Department managed by a head. | NOT_AVAILABLE
- head_ID | INT | NONE | NONE | Head managing the department. | NOT_AVAILABLE
- temporary_acting | TEXT | NONE | NONE | Whether the head is temporary acting. | NOT_AVAILABLE

Question:
Which department currently headed by a temporary acting manager has the largest number of employees, and how many employees does it have?

Hint (may be empty):
No hint

response:
{
  "relevant_columns": {
    "department": ["Department_ID", "Name", "Num_Employees"],
    "management": ["department_ID", "temporary_acting"]
  }
}

### Example 2:
[DATABASE_ID]
activity_1

[SELECTED_TABLES]
- Student
- Participates_in
- Activity

[FOREIGN_KEY_RELATIONSHIPS_WITHIN_SELECTED_TABLES]
- Participates_in.stuid -> Student.StuID
- Participates_in.actid -> Activity.actid

[TABLE] Student
- StuID | integer | PRIMARY KEY | NONE | Unique identifier for a student. | 1001, 1002, 1003
- LName | text | NONE | NONE | Last name of the student. | Smith, Kim, Jones
- Fname | text | NONE | NONE | First name of the student. | Linda, Tracy, Shiela
- Age | integer | NONE | NONE | Age of the student. | 18, 19, 21
- Sex | text | NONE | NONE | Gender of the student. | F, M
- Major | integer | NONE | NONE | Major code of the student. | 600
- Advisor | integer | NONE | NONE | Faculty ID of the student's advisor. | 1121, 7712, 7792
- city_code | text | NONE | NONE | City code associated with the student. | BAL, HKG, WAS

[TABLE] Participates_in
- stuid | integer | FOREIGN KEY | Student.StuID | Foreign key referencing the Student table, indicating the student participating. | 1001, 1002
- actid | integer | FOREIGN KEY | Activity.actid | Foreign key referencing the Activity table, indicating the activity the student participates in. | 770, 771, 777

[TABLE] Activity
- actid | integer | PRIMARY KEY | NONE | Unique identifier for an activity. | 770, 771, 772
- activity_name | text | NONE | NONE | Name of the activity. | Mountain Climbing, Canoeing, Kayaking

Question:
Which activities does student Linda Smith participate in?

Hint (may be empty):
No hint

response:
{
  "relevant_columns": {
    "Student": ["StuID", "LName", "Fname"],
    "Participates_in": ["stuid", "actid"],
    "Activity": ["actid", "activity_name"]
  }
}

### Example 3:
[DATABASE_ID]
ADVENTUREWORKS

[SELECTED_TABLES]
- ADVENTUREWORKS.ADVENTUREWORKS.CURRENCYRATE

[FOREIGN_KEY_RELATIONSHIPS_WITHIN_SELECTED_TABLES]
NOT_AVAILABLE

[TABLE] ADVENTUREWORKS.ADVENTUREWORKS.CURRENCYRATE
- currencyrateid | INT | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | 2, 4, 5
- tocurrencycode | STRING | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | AUD, CAD, CNY
- endofdayrate | FLOAT | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | 1.55, 1.4683, 8.2784
- modifieddate | TIMESTAMP | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | 2011-05-31 00:00:00
- currencyratedate | TIMESTAMP | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | 2011-05-31 00:00:00
- fromcurrencycode | STRING | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | USD
- averagerate | FLOAT | NOT_AVAILABLE | NOT_AVAILABLE | NOT_AVAILABLE | 1.5491, 1.4641, 8.2781

Question:
What are the average and end-of-day exchange rates from USD to CAD?

Hint (may be empty):
No hint

response:
{
  "relevant_columns": {
    "ADVENTUREWORKS.ADVENTUREWORKS.CURRENCYRATE": ["fromcurrencycode", "tocurrencycode", "averagerate", "endofdayrate"]
  }
}

### INPUT:
{DATABASE_SCHEMA}

Question:
{QUESTION}

Hint (may be empty):
{HINT}
